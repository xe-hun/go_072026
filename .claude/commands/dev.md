Activate `/dev` for this session.

Follow `AGENTS.md`: use development-stage implementation rules and implement requested changes directly without backward compatibility or legacy support unless the user explicitly requests it.
